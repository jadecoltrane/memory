# Obsidian 配置(主题、插件、CSS 片段等)入库跟着 git 走两端同步;
# 只排除纯设备本地状态:窗口布局/打开的标签页(两端各自不同,同步了只会
# 产生噪音提交甚至冲突)和缓存(体积大、无同步价值)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/

# obsidian-git 插件自身的行为设置(2026-07-07):跟同步节奏强相关,
# 手机和 Mac 需要不同的 autoBackupAfterFileChange/autoPullOnBoot 等
# 组合(手机密集接收 Mac 这边的重命名/删除提交时,"文件一变动就自动
# commit"会把本地残留的旧文件当"新增"重新提交回去,跟远端的删除打架,
# 产生一堆虚假冲突),两端各自管理,不再同步这份配置——跟上面
# workspace.json 是同一类"设备本地状态"。移除同步后两端第一次都会
# 从默认设置重新来,需要各自手动调一遍
.obsidian/plugins/obsidian-git/data.json

# 图谱视图的缩放/位置(2026-07-07):scale 这类纯视图状态几乎每次打开
# 图谱都会变,两端一同步就冲突(2026-07-07 当晚已经因为这个冲突过一次)。
# 过滤器/配色分组等"设计决定"也在这个文件里、跟缩放没法拆开单独同步,
# 一并不再入库,两端各自的图谱外观独立维护——跟上面 workspace.json
# 是同一类问题同一种处理方式
.obsidian/graph.json

# Claudian 插件的本地配置与会话缓存,不是记忆内容,不入库
claudian-settings.json
conv-*.json
conv-*.jsonl

# macOS 系统文件
.DS_Store
