---
cssclasses: workbench
generated: 2026-07-20
painting-url: https://upload.wikimedia.org/wikipedia/commons/2/2b/Diego_Vel%C3%A1zquez_-_Las_Meninas_or_The_Family_of_Philip_IV_-_WGA24447.jpg
painting-title: 迭戈·委拉斯凯兹《宫娥》(1656)
painting-intro: 委拉斯凯兹把自己也画进了这幅群像:小公主站在正中央,画家本人举着画笔立在巨大画布前回望观者,背景的镜子里隐约映出国王夫妇的身影——到底谁在看谁、画布正面画的又是谁,四百年来被反复讨论。这幅画是西班牙黄金时代绘画的巅峰之作,现藏于马德里普拉多博物馆。
---

```dataviewjs
// —— 信息条:问候(左) / 实时时钟(右) ——
// 2026-07-05 按手绘草图要求调整:问候语挪到画作上方,不再跟画作拼一块
// 卡片、也不再有自己的卡片背景,就是页面顶部的一段纯文字页眉。
// 2026-07-06 拆出独立代码块:之前跟画作横幅共用同一个顶层容器,画作
// 想跟"随机重逢"横排成一行就得靠 CSS 收窄这整个顶层容器的宽度,会连带
// 把问候语/时钟也挤扁——拆开后问候条继续占满整行,画作横幅是单独一个
// 顶层块,才能只收窄它自己去跟随机重逢并排。
const bar = dv.container.createEl("div", { cls: "wb-infobar" });
const left = bar.createEl("div");
const greetEl = left.createEl("div", { cls: "wb-greet" });
const dateEl = left.createEl("div", { cls: "wb-date" });
const clockEl = bar.createEl("div", { cls: "wb-clock" });
const wd = ["周日", "周一", "周二", "周三", "周四", "周五", "周六"];
const tick = () => {
  const d = new Date(), h = d.getHours();
  greetEl.setText(h < 5 ? "夜深了,Jade" : h < 11 ? "早安,Jade" : h < 13 ? "午安,Jade" : h < 18 ? "下午好,Jade" : "晚上好,Jade");
  dateEl.setText(`${d.getFullYear()} 年 ${d.getMonth() + 1} 月 ${d.getDate()} 日 · ${wd[d.getDay()]}`);
  clockEl.setText(`${String(d.getHours()).padStart(2, "0")}:${String(d.getMinutes()).padStart(2, "0")}`);
};
tick();
// 用 dv.component.registerInterval 而不是裸 setInterval——
// 裸 setInterval 在切到二级页再返回时不会被自动清理,每次返回都会
// 叠加一个新计时器,旧计时器仍在写已经被 Dataview 替换掉的旧 DOM 节点,
// 时间久了(尤其内存更紧张的手机端)就会拖累这个页面重新渲染,表现为
// "回来后展示不正常"。registerInterval 把计时器生命周期绑定到这次
// 渲染的组件上,组件卸载时会自动清掉,不再累积。
dv.component.registerInterval(setInterval(tick, 15000));
```

```dataviewjs
const p = dv.current();
// 不能用 document.querySelector(".workbench")——Obsidian 同时把 reading
// view 和 source view 两个容器都挂在 DOM 里(只是不显示的那个
// display:none),两个容器都带 workbench 这个类,querySelector 永远只拿
// 第一个匹配,可能拿到的是当前隐藏不可见的那个,样式就白设了。用
// dv.container.closest(".workbench") 才能保证拿到"这次渲染实际所在"的
// 那个容器(2026-07-05 用 devtools 实测定位到)。
const root = dv.container.closest(".workbench");

// —— 页面背景:纸感渐变 ——
// 2026-07-07 撤销"渐变跟随画作主色调"(2026-07-06 加的那版):跟随
// 画作意味着渐变要等画从 Wikimedia 加载完、canvas 取完色才能定稿,
// 慢网络下这段等待会让背景先出现兜底色、过一会儿又跳一次,读起来就是
// "加载得很慢"。改回渐变完全不依赖画作/网络,只按"当天日期"为种子
// 随机生成三个色相做三角配色,页面一打开就是最终颜色,不会再变
// (取色回调那边现在只更新 --wb-accent,不再碰这份渐变)。
// 饱和度和亮度都锁死在"很浅的粉彩"区间(饱和 60%~95%、亮度 89%~94%),
// 保证不管什么色相都是柔和纸感,不会出脏色或深色。随机数用 mulberry32,
// 同一天内反复打开颜色不变,跨天自动换。
let seed = Math.floor(Date.now() / 86400000);
const rand = () => {
  seed = (seed + 0x6D2B79F5) | 0;
  let t = Math.imul(seed ^ (seed >>> 15), 1 | seed);
  t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
  return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
};
const pastel = (h) => `hsl(${Math.round((h % 360 + 360) % 360)}, ${Math.round(60 + rand() * 35)}%, ${Math.round(89 + rand() * 5)}%)`;
// 三色按"三角配色"(triadic,互相间隔约 120°)取,而不是让第二、三色
// 挨着第一色小幅漂移——2026-07-06 排查发现:之前 stop2/stop3 的偏移量
// (+20~60、+55~110)其实是同一段连续区间,三个色相挤在 110° 以内的
// 一段弧上,如果起点落在蓝色一带,漂着漂着全都漂进了紫/品红,看起来
// 就是"一坨蓝紫粉",而不是像原来手工预设那样跨色相的三色(桃杏+粉+蓝)。
// 120°/240° 间隔能保证三色互相拉开,同时其中一色落在品红/粉附近是三角
// 配色从蓝色出发的正常结果(原手工预设本身也是蓝配粉),不是 bug。
const triadGradient = (h0) => {
  const stops = [pastel(h0), pastel(h0 + 100 + rand() * 40), pastel(h0 + 220 + rand() * 40)];
  return `linear-gradient(135deg, ${stops[0]} 0%, ${stops[1]} ${Math.round(40 + rand() * 20)}%, ${stops[2]} 100%)`;
};
root?.style.setProperty("--wb-page-gradient", triadGradient(rand() * 360));

// —— 横幅:每日画作,独立一张卡片 ——
// 2026-07-06 改掉了固定宽高比裁切的横条 banner,画名+简介挪到画的旁边
// (宽屏)/下方(窄屏),不再压在画上。
// 2026-07-06 二次修复:第一版沿用了旧横幅设计的 class 名
// (wb-banner / wb-painting-caption 等),本地 .obsidian/snippets/
// workbench.css 里给旧设计写的"文字绝对定位叠在图上"规则照样命中
// 同名 class,新结构在视觉上被旧样式接管,看起来跟没改一样。这次全部
// 换成这个改动专属、旧样式表不可能定义过的新 class 名,并且把外层容器
// 的box形状属性(height/overflow/position/aspect-ratio)也显式内联
// 覆盖掉,不只是加新属性——防止旧 .wb-banner 规则里可能存在的固定高度
// /overflow:hidden 继续把外层容器锁成横条形状。
// 2026-07-06 三次调整:跟"随机重逢"横排成一行(见 CSS 里对
// :has(.wb-daily-painting) 的 flex-basis),配合改成毛玻璃质感——
// 半透明底 + backdrop-filter 模糊,并去掉阴影(用户明确要求"没有阴影",
// 边界完全交给模糊的半透明底自己撑,不再靠投影)。height:100% 是为了
// 在同一行里跟"随机重逢"卡片(它自己也是 height:100%)等高。
const banner = dv.container.createEl("div", { cls: "wb-daily-painting" });
const narrowScreen = window.matchMedia("(max-width: 520px)").matches;
// 背景/圆角/模糊/阴影不再写进这里的内联样式,挪到 workbench.css 的
// .wb-daily-painting class 规则里统一管理(2026-07-06 四次调整)——
// 内联样式的优先级天然盖过任何 CSS 选择器,包括 :hover,之前把这几个
// 外观属性写死在这儿,导致 CSS 里加的 hover 效果永远生效不了。这里只
// 留这次渲染动态算出来的结构性属性(横竖排、对齐方式这些随窄屏/宽屏
// 变化的值),外观交给 CSS class 才能让 hover/active 状态正常切换。
// 2026-07-06 五次调整:align-items 从 center 改成 flex-start——用户要求
// 画名/简介的文字顶部要跟右边"随机重逢"卡片的标题顶部对齐,center 会把
// 图+文字整体在竖直方向居中,文字实际起始位置比画的中线还低,对不齐
// 2026-07-07 高度主从关系反过来:height 从 100% 改 auto——之前 100%
// 是让画作卡被撑到跟"随机重逢"等高,但撑出来的多余高度图片吃不掉,
// 全变成画作上下的大片空白(用户圈图指出)。现在画作卡按内容收缩,
// 高度就是图片+上下 16px padding;"随机重逢"靠 syncReunionHeight 的
// ResizeObserver 反过来跟画作等高。workbench.css 里 :has(.wb-daily-
// painting) 那组"逐层撑满"规则同步删掉了(只留给随机重逢用)
banner.style.cssText = [
  "display:flex",
  `flex-direction:${narrowScreen ? "column" : "row"}`,
  `align-items:${narrowScreen ? "stretch" : "flex-start"}`,
  "gap:16px",
  "height:auto", "box-sizing:border-box", "min-height:0", "max-height:none", "aspect-ratio:auto",
  "overflow:visible", "position:static", "padding:16px",
].join(";");

const imgWrap = banner.createEl("div", { cls: "wb-daily-painting-imgwrap" });
// 2026-07-07 二次修:画作卡不再被外层 flex 拉高(banner 改 height:auto,
// 见上),之前为"消化多余高度"加的 align-self:stretch + flex 垂直居中
// 随之失去意义,一并删掉——图片顶部贴着 16px padding,跟右边药丸/标题
// 块同一条顶线,画作上下留白自然就是 padding 那 16px(用户要求"画作
// 上下只留右边标题到上方的距离")
imgWrap.style.cssText = `text-align:center; position:static; flex:0 ${narrowScreen ? "0 auto" : "1 46%"};`;
const bimg = imgWrap.createEl("img", { cls: "wb-daily-painting-img" });
if (p["painting-url"]) bimg.src = p["painting-url"];
bimg.style.cssText = `max-width:100%; max-height:${narrowScreen ? "260px" : "300px"}; border-radius:12px; display:inline-block; position:static;`;

// 2026-07-06 调整顺序:作品名(大)在上,简介/见解(小)在下,像美术馆
// 标签先报作品名再补一句说明,不是反过来
const caption = banner.createEl("div", { cls: "wb-daily-painting-side" });
caption.style.cssText = `position:static; min-width:160px; flex:${narrowScreen ? "0 0 auto" : "1 1 200px"};`;
// 2026-07-07 用户要求加个跟"🎲 随机重逢"对应的药丸标签,两张卡视觉上
// 才对称——直接复用 wb-reunion-tag 这个 class(纯样式,不含任何跟
// 随机重逢功能绑定的东西),CSS 里把选择器扩成同时覆盖两个 class 名。
// 标签外面包一层 wb-painting-head。2026-07-07 二次调整:这行曾经用
// min-height:40px 跟随机重逢的标签行(被 40px 换一篇按钮撑高)等高、
// 让两张卡的标题对到同一水平线,代价是药丸在 40px 行里垂直居中、上方
// 挤出一截空白(用户圈图指出)。现在改成两边药丸都贴着卡片 16px
// padding 的顶线对齐(CSS 里删了 min-height,随机重逢头部 align-items
// 改 flex-start),标题不再强求同一水平线
const paintingHead = caption.createEl("div", { cls: "wb-painting-head" });
paintingHead.createEl("span", { cls: "wb-reunion-tag wb-painting-tag", text: "🖼 今日画作" });
const titleEl = caption.createEl("div", { cls: "wb-daily-painting-name", text: p["painting-title"] ?? "" });
titleEl.style.cssText = "font-weight:600; font-size:1.05em; line-height:1.4; margin-bottom:8px; position:static; background:none; text-shadow:none;";
const introEl = caption.createEl("div", { cls: "wb-daily-painting-desc", text: p["painting-intro"] ?? "" });
introEl.style.cssText = "font-size:0.92em; line-height:1.6; opacity:0.75; position:static; background:none; text-shadow:none;";

// 客户端取画作主色调写进 --wb-accent(拿不到就沿用 CSS 兜底色)。
// 2026-07-05 UI 评审从"平均色"改成"主色调":平均会让所有颜色互相抵消,
// 不管什么画算出来都是灰紫/灰褐(星夜的平均就是浑浊的灰蓝)。做法:
// 有饱和度的像素按色相分 12 桶投票,取票数最多的桶内均值;文字可读性
// 交给 CSS 里 OKLCH 派生色阶去锁亮度,这里不再手动提亮。
if (p["painting-url"]) {
  const img = new Image();
  img.crossOrigin = "anonymous";
  img.onload = () => {
    try {
      const c = document.createElement("canvas");
      c.width = 32; c.height = 32;
      const ctx = c.getContext("2d");
      ctx.drawImage(img, 0, 0, 32, 32);
      const d = ctx.getImageData(0, 0, 32, 32).data;
      const bins = Array.from({ length: 12 }, () => ({ r: 0, g: 0, b: 0, n: 0 }));
      for (let i = 0; i < d.length; i += 4) {
        const r = d[i], g = d[i + 1], b = d[i + 2];
        const mx = Math.max(r, g, b), mn = Math.min(r, g, b);
        if (mx - mn < 24) continue; // 近灰的像素没有可用色相,不参与投票
        const h = mx === r ? ((g - b) / (mx - mn) + 6) % 6 : mx === g ? (b - r) / (mx - mn) + 2 : (r - g) / (mx - mn) + 4;
        const bin = bins[Math.floor(h * 2) % 12]; // h∈[0,6) → 12 桶,每桶 30°
        bin.r += r; bin.g += g; bin.b += b; bin.n++;
      }
      const top = bins.reduce((a, x) => (x.n > a.n ? x : a));
      // 整幅都近灰(top.n 为 0)就不写,强调色沿用兜底
      if (top.n) {
        const R = Math.round(top.r / top.n), G = Math.round(top.g / top.n), B = Math.round(top.b / top.n);
        root?.style.setProperty("--wb-accent", `rgb(${R}, ${G}, ${B})`);
        // 2026-07-07 撤销"页面渐变跟随画作主色调"(2026-07-06 加的):
        // 用户反馈渐变背景总是加载得很慢——真正的原因是这里依赖画作
        // 图片先从 Wikimedia 网络加载完、canvas 取色算完,才会把渐变从
        // 兜底色换成画作色,慢网络下这段等待时间就是"背景突然变色"的
        // 延迟感。渐变改回只用上面 rand() 种子直接生成的三色三角配色,
        // 跟画作/网络完全解耦,页面一打开就是最终颜色,不会再变。
        // 强调色(按钮/文字高亮用的 --wb-accent)还留着跟随画作,这条
        // 不受影响——只是不会再拿它去覆盖渐变了。
      } else {
        console.warn("[工作台] 取色失败:整幅画在采样点都太接近灰色,没有可投票的色相,沿用兜底色 --wb-accent");
      }
    } catch (e) {
      // 2026-07-07 改:之前这里完全静默吞掉异常,导致强调色一直卡在
      // CSS 兜底的灰褐色(#8a8578)却查不出原因——现在打印真实错误,
      // 大概率是 img.crossOrigin 设了但图源没回 Access-Control-Allow-Origin
      // 头(canvas 被"跨域污染",getImageData 直接抛 SecurityError)
      console.warn("[工作台] 画作取色失败,沿用兜底色 --wb-accent:", e);
    }
  };
  img.onerror = (e) => {
    // 之前没有 onerror,图片加载失败(404/超时/CORS 预检失败)时
    // onload 根本不会触发,取色逻辑连跑都不会跑,而且不报任何错——
    // 表现上跟"取色成功但算出兜底同款颜色"一模一样,不打日志根本
    // 分不清是这里失败还是上面 try/catch 里失败
    console.warn("[工作台] 画作图片加载失败,取色跳过,沿用兜底色 --wb-accent。图片地址:", p["painting-url"], e);
  };
  img.src = p["painting-url"];
}

// —— 心跳:超过 48 小时没重新生成,说明每日任务可能挂了 ——
const gen = p.generated;
const genMs = gen?.toMillis ? gen.toMillis() : (gen ? new Date(String(gen)).getTime() : NaN);
const ageH = isNaN(genMs) ? Infinity : (Date.now() - genMs) / 36e5;
if (ageH > 48) dv.container.createEl("div", {
  cls: "wb-heartbeat",
  text: `⚠️ 工作台已 ${isFinite(ageH) ? Math.floor(ageH / 24) + " 天" : "很久"}没更新,每日任务可能没在跑`,
});
```

```dataviewjs
// —— 随机重逢:独立卡片,带摘要预览,可换一篇 ——
// 排除 notes/问答/:那是每日工作台问题的归档记录(见 CLAUDE.md
// 「收工时」归档规则),不是用户自己收藏的文章/知识点,不该被当成
// "重逢"的旧笔记抽出来(2026-07-07 用户要求)
const open = (path) => app.workspace.openLinkText(path, "/", false);
const renderReunion = async (container) => {
  container.innerHTML = "";
  const arr = dv.pages('"notes"').where(p => !p.file.folder.startsWith("notes/问答")).array();
  if (!arr.length) return;
  const pick = arr[Math.floor(Math.random() * arr.length)];
  const head = container.createEl("div", { cls: "wb-reunion-head" });
  head.createEl("span", { cls: "wb-reunion-tag", text: "🎲 随机重逢" });
  // 图标包进 span:CSS 要用 background-clip: text 给它做渐变填充
  // (2026-07-06 按用户给的 figma 拟物按钮教程重做,样式见 CSS)
  const reroll = head.createEl("button", { cls: "wb-reunion-reroll" });
  const rerollGlyph = reroll.createSpan({ cls: "wb-reroll-glyph", text: "↻" });
  // 2026-07-06 九次修复:之前这里钉死内联 rotate(0deg) 是为了压住本地
  // CSS :hover 规则里可能残留的旋转角度(手机点击后残留 hover 状态)。
  // 现在用户要求恢复"鼠标划上去转一圈"的 hover 效果(CSS 里
  // .wb-reunion-reroll:hover .wb-reroll-glyph 已改回 rotate(360deg))——
  // 内联样式优先级天生盖过任何 CSS 选择器,只要这里还留一个内联
  // transform,hover 状态的 CSS 规则就永远生效不了。改成不设内联值,
  // 把 transform 完全交给 CSS 控制:静息态没有内联也没有非 hover 的
  // CSS 规则,等效 rotate(0deg);hover 时 CSS 规则接管转到 360deg——
  // 而且 360deg 和 0deg 视觉上是同一个角度,即使手机残留 hover 状态,
  // 图标看起来也和不转时一模一样,不会像旧版 180deg 那样卡成倒转的样子。
  container.createEl("div", { cls: "wb-reunion-title", text: pick.file.name });
  const excerptEl = container.createEl("div", { cls: "wb-reunion-excerpt" });
  const excerptTextEl = excerptEl.createEl("div", { cls: "wb-reunion-excerpt-text", text: "…" });
  container.onclick = () => open(pick.file.path);
  // 2026-07-06 修复手机上点一下正转、再点一下反着转回去的问题:旧写法
  // 把旋转挂在 :hover 上,手机没有真正的 hover,来回摆动而不是一直往前
  // 转;改成 Web Animations API 播一次 0→360° 正向旋转。
  // 2026-07-06 二次修复:光换成 animate() 还不够——下面 renderReunion
  // 会同步清空并重建整张卡片(含这个按钮本身),动画刚触发、浏览器还
  // 没画出一帧,承载动画的 DOM 节点就被销毁重建了,等于根本没转出来。
  // 现在先 await 动画真正播完(anim.finished)再重建卡片内容;重建前
  // 顺手把按钮禁用一下,防止动画播放期间连续点击又并发触发第二次。
  reroll.onclick = async (e) => {
    e.stopPropagation();
    if (reroll.disabled) return;
    reroll.disabled = true;
    const anim = rerollGlyph.animate(
      [{ transform: "rotate(0deg)" }, { transform: "rotate(360deg)" }],
      { duration: 450, easing: "ease" }
    );
    try { await anim.finished; } catch (_) { /* 动画被打断也继续换下一篇 */ }
    // Web Animations API 播完的效果本来就会自动回落(fill 默认不是
    // forwards),不需要再手动写回 rotate(0deg)——写了反而会重新钉死
    // 内联样式,把上面刚恢复的 CSS hover 效果又压回去。这里改成清掉
    // 上面 animate() 可能残留在 style 属性上的 transform(以防个别端
    // 实现细节导致没完全清空),交还控制权给 CSS。
    rerollGlyph.style.removeProperty("transform");
    renderReunion(container);
  };
  const raw = await dv.io.load(pick.file.path).catch(() => "");
  const body = (raw || "")
    .replace(/^---[\s\S]*?---/, "")
    .replace(/\[\[([^\]|]*)\|?[^\]]*\]\]/g, "$1")
    .replace(/[#>*`_]/g, "")
    .trim();
  // 2026-07-07 二次调整:500 字配 8 行 line-clamp,对短笔记来说经常
  // 不够填满卡片——卡片高度是跟着画作横幅走的(见下面 syncReunionHeight),
  // 画作越高、这个上限就越容易先于真实内容耗尽,摘要文字量不够,剩下
  // 的空间只能空着,视觉上就是"下面一大截间距"。用户要求"多给点文字,
  // 让文字自适应"——改成给一个宽松得多的上限(1500 字),配合 CSS 里
  // 同步调高的 line-clamp(8→14 行),多数笔记的正文都够填满这个量级;
  // 这仍然是"给够料、交给 CSS 按实际高度自然裁"的思路,不引入任何 JS
  // 测量,笔记本身短于这个上限时就老实显示全文,不会被硬凑
  const snippet = body.slice(0, 1500).replace(/\s+/g, " ").trim();
  excerptTextEl.setText(snippet + (body.length > 1500 ? "…" : ""));
};
const reunionEl = dv.container.createEl("div", { cls: "wb-reunion" });
renderReunion(reunionEl);

// —— 卡片高度跟着名画横幅走(2026-07-07 用户要求)——
// 之前靠摘要字数硬凑高度去贴近画作,本质是猜:画作横竖、笔记长短
// 每天不一样,笔记一长、画作一矮就会反过来把画作卡片撑出一截空白
// (画作卡片自己没设高度,被 flex 拉伸到跟随机重逢一样高,但里面的
// <img> 不会跟着拉伸,多出来的高度就是纯空白)。改用 ResizeObserver
// 实时量画作卡片(.wb-daily-painting)的真实渲染高度,写成随机重逢
// 卡片的 max-height,配合 CSS 里的 overflow:hidden + line-clamp 裁掉
// 超出的文字。窄屏(≤520px,跟 narrowScreen 同一断点)两卡各占一行
// 堆叠,不存在"跟着谁的高度走"这回事,清掉 max-height 让它按内容
// 自然展开。ResizeObserver 而不是一次性测量,是因为图片是异步加载的
// (onload 之前画作卡片还是塌陷的),观察者能在图片真正撑开卡片高度
// 之后自动补一次同步,不用猜时序。
const syncReunionHeight = () => {
  const painting = document.querySelector(".workbench .wb-daily-painting");
  if (!painting) return;
  if (window.innerWidth <= 520) {
    reunionEl.style.maxHeight = "";
    return;
  }
  const h = painting.getBoundingClientRect().height;
  if (h > 0) reunionEl.style.maxHeight = h + "px";
};

// —— 名画标题/随机重逢标题的对齐(2026-07-07 简化,撤销 JS 实测方案)——
// 之前折腾过一版"用 JS 实测两个标题的真实坐标、动态算 marginTop 强行
// 对到同一条水平线"的方案,结果引入了新 bug:syncCaptionAlign 给画作
// 说明文字块加的 marginTop 会把 .wb-daily-painting 这张卡的实际高度
// 撑高,但 syncReunionHeight 是按"加高之前"量出来的画作高度去设随机
// 重逢卡片的 max-height,两边算的不是同一份高度,导致两张卡实际渲染
// 出来反而对不上高。用户反馈后明确简化了要求:不需要两个标题严丝合缝
// 对在同一条线上,只要"标题到正文"的间距在两张卡里一致就行——这是
// 纯 CSS 的事,不需要量任何坐标,已经在 workbench.css 里把
// .wb-reunion-title 的 margin-bottom 和画作 titleEl 的 margin-bottom
// 对成同一个值(8px),这里不再需要任何 JS。
const syncLayout = syncReunionHeight;
// 沿用同样的"轮询等画作块 DOM 就绪"写法(见上一版改动的教训:两个
// dataviewjs 块之间不保证渲染顺序,只在脚本执行的"这一刻"查一次可能
// 扑空)——虽然现在只剩 syncReunionHeight 一件事要做,这个时序问题
// 本身没有消失,还是得等真正找到画作卡片才挂 ResizeObserver
const setupLayoutSync = (triesLeft = 34) => {
  const paintingEl = document.querySelector(".workbench .wb-daily-painting");
  if (!paintingEl) {
    if (triesLeft > 0) setTimeout(() => setupLayoutSync(triesLeft - 1), 150);
    else console.warn("[工作台] 等了 5 秒还是没找到画作卡片的 DOM,放弃高度同步——可能画作代码块本身报错了");
    return;
  }
  syncLayout();
  // 2026-07-07 修:之前这个 ResizeObserver 和 resize 监听器从没清理过,
  // 跟上面 tick() 定时器踩过的是同一个坑(见文件顶部那段注释)——这个
  // dataviewjs 块只要重新渲染一次(切页面再回来、或文件被外部改动触发
  // Dataview 重渲),就会在新一份 paintingEl 上再挂一对新的观察者,旧的
  // 那对还在盯着可能已经从 DOM 摘掉的旧画作卡,两份回调谁后触发就由谁
  // 把 reunionEl.style.maxHeight 写成谁量到的值,新旧数值互相打架,表现
  // 就是"随机重逢的高度跟画作对不上"。改成用 dv.component.register 把
  // disconnect/removeEventListener 绑定到这次渲染的生命周期上,组件卸载
  // (离开/重渲染这个块)时自动清掉,不再累积。
  if (window.ResizeObserver) {
    const ro = new ResizeObserver(syncLayout);
    ro.observe(paintingEl);
    dv.component.register(() => ro.disconnect());
  }
  window.addEventListener("resize", syncLayout);
  dv.component.register(() => window.removeEventListener("resize", syncLayout));
};
setupLayoutSync();
```

```dataviewjs
// —— 胶囊标签栏 + 页内面板(2026-07-07 按用户手绘草图重做)——
// 旧版统计卡点一下是跳去全局搜索/图谱(二级页),草图要求:点胶囊直接
// 在同一页面下方展开对应内容面板,再点同一颗收起,全程不离开工作台。
// 胶囊按草图定为 笔记/概念/双链/本周新增/最近编辑 五颗(待复核胶囊
// 随之去掉——复核提醒还在 GC 和每日生成流程里,不依赖这个入口)。
// ➕ 新建笔记挪到最右,样式是普通胶囊(第一版做成圆形玻璃按钮,用户
// 反馈"还是要胶囊的样子"改回来了),只在图标 span 上单独转一圈。
//
// 性能:dv.pages() 读的是 Dataview 已经建好的内存索引,不重新扫盘;
// 下面的计数/reduce 都是几千条量级也是毫秒内的数组操作;真正会拖慢的
// 是"把全部结果一次性画成 DOM",所以面板一律走 renderRows 的默认
// 8 条上限(见下方),笔记再多也不会一次性铺出几百个节点。
const open = (path) => app.workspace.openLinkText(path, "/", false);
const openGraph = () => app.commands.executeCommandById("graph:open");
const newNote = async () => {
  const folder = "notes";
  if (!app.vault.getAbstractFileByPath(folder)) await app.vault.createFolder(folder);
  let n = 1, path;
  do { path = `${folder}/未命名笔记 ${n++}.md`; } while (app.vault.getAbstractFileByPath(path));
  const now = new Date();
  const today = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, "0")}-${String(now.getDate()).padStart(2, "0")}`;
  const file = await app.vault.create(path, `---\ntype: note\ncreated: ${today}\n---\n\n`);
  await app.workspace.getLeaf(true).openFile(file);
};

const notes = dv.pages('"notes"'), concepts = dv.pages('"concepts"');
const all = [...notes.array(), ...concepts.array()];
const edges = all.reduce((n, pg) => n + pg.file.outlinks.length, 0);
// created/mtime 都是 luxon DateTime,.ts 取毫秒;created 没写 frontmatter
// 时退回文件系统的 ctime,保证"本周新增/日期列"不至于空白
const ctimeOf = (pg) => pg.created?.ts ?? (pg.created ? new Date(String(pg.created)).getTime() : pg.file.ctime?.ts ?? 0);
const mtimeOf = (pg) => pg.file.mtime?.ts ?? 0;
const weekNewPages = notes.array().filter(pg => {
  const t = ctimeOf(pg);
  return t > 0 && Date.now() - t < 7 * 864e5;
});
const fmt = (ms) => {
  if (!ms) return "";
  const d = new Date(ms);
  return `${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
};

// 通用行列表:标题在左、右侧一列小字(日期或入链数),整行可点开笔记。
// 默认只画前 limit 条,超出的用一行"展开全部(N)"收着,点了再铺开、
// 再点一次收回去——不做真分页,单次展开够用。调用方如果提前把数组
// 截到 <= limit(比如"最近编辑"故意只给 8 条),这个按钮不会出现,
// 因为已经没有"更多"可展开了。
const renderRows = (host, arr, rightOf, limit = 8) => {
  if (!arr.length) { host.createEl("div", { cls: "wb-panel-empty", text: "还没有内容" }); return; }
  const listEl = host.createEl("div");
  let expanded = false;
  const draw = () => {
    listEl.innerHTML = "";
    const shown = expanded ? arr : arr.slice(0, limit);
    for (const pg of shown) {
      const row = listEl.createEl("div", { cls: "wb-panel-row" });
      row.createEl("span", { cls: "wb-panel-row-title", text: pg.file.name });
      row.createEl("span", { cls: "wb-panel-row-date", text: rightOf(pg) });
      row.onclick = () => open(pg.file.path);
    }
  };
  draw();
  if (arr.length > limit) {
    const more = host.createEl("div", { cls: "wb-panel-more", text: `展开全部(${arr.length})` });
    more.onclick = () => {
      expanded = !expanded;
      more.setText(expanded ? "收起" : `展开全部(${arr.length})`);
      draw();
    };
  }
};

// 生成/刷新一份 Obsidian 原生 Bases 数据库文件:一个视图放全部笔记,
// 其余每个分类一个视图,过滤条件是 file.inFolder("notes/分类名")
// (未分类用 file.folder == "notes" 精确匹配,inFolder 是递归的,用它
// 会把所有子分类也扫进来)。放在 meta/ 下(基础设施产物,不是用户笔记,
// 不该混进 notes/)。每次打开笔记面板都重新整份重写——分类是从 notes/
// 实时扫出来的,新增/改名子文件夹后数据库视图要跟着变,不能手写死一份
// 等着过期(懒加载:只在用户真的点开笔记面板这一刻才重新生成,不做成
// 后台常驻任务)
const NOTES_BASE_PATH = "meta/笔记数据库.base";
const buildNotesBase = async (catGroups) => {
  const esc1 = (s) => s.replace(/'/g, "''"); // YAML 单引号字符串里,单引号本身要写成两个单引号转义
  const esc2 = (s) => s.replace(/"/g, '\\"');
  const lines = ["views:"];
  const pushView = (name, filterExpr) => {
    lines.push("  - type: table");
    lines.push(`    name: "${esc2(name)}"`);
    lines.push("    filters:");
    lines.push("      and:");
    lines.push(`        - '${esc1(filterExpr)}'`);
  };
  pushView("全部笔记", `file.inFolder("notes")`);
  for (const [name] of catGroups) {
    const filterExpr = name === "未分类" ? `file.folder == "notes"` : `file.inFolder("notes/${name}")`;
    pushView(name, filterExpr);
  }
  await app.vault.adapter.write(NOTES_BASE_PATH, lines.join("\n") + "\n");
};

// 笔记面板:2026-07-07 改版——只留分类方卡 + 搜索框两件事(不再点开
// 分类就在下面铺一份行列表)。点分类现在打开的是上面生成的 Bases
// 数据库视图(原生表格,自带排序/按属性筛选,比自己拼一份静态行列表
// 更有用),搜索框保留即时过滤(输入才出结果,清空就什么都不显示,
// 不再有"默认选中第一类"的常驻列表)
const notesPanel = async (host) => {
  const groups = new Map();
  for (const pg of notes) {
    const seg = pg.file.folder.split("/");
    const cat = seg.length > 1 ? seg[1] : "未分类";
    if (!groups.has(cat)) groups.set(cat, []);
    groups.get(cat).push(pg);
  }
  const sorted = [...groups.entries()].sort((a, b) => b[1].length - a[1].length);
  await buildNotesBase(sorted);

  const searchWrap = host.createEl("div", { cls: "wb-panel-search" });
  const searchInput = searchWrap.createEl("input", { cls: "wb-panel-search-input" });
  searchInput.type = "text";
  searchInput.placeholder = "搜标题…";

  const catRow = host.createEl("div", { cls: "wb-cat-grid" });
  for (const [name, arr] of sorted) {
    const card = catRow.createEl("div", { cls: "wb-cat-card", attr: { title: name } });
    card.createEl("div", { cls: "wb-cat-num", text: String(arr.length) });
    card.createEl("div", { cls: "wb-cat-name", text: name });
    card.onclick = () => open(`${NOTES_BASE_PATH}#${name}`);
  }

  const resultHost = host.createEl("div");
  searchInput.oninput = () => {
    const q = searchInput.value.trim().toLowerCase();
    resultHost.innerHTML = "";
    if (!q) return;
    const hits = notes.array()
      .filter((pg) => pg.file.name.toLowerCase().includes(q))
      .sort((a, b) => mtimeOf(b) - mtimeOf(a));
    renderRows(resultHost, hits, (pg) => fmt(mtimeOf(pg)));
  };
};

// 概念面板:2026-07-07 改版——跟笔记分类同一套小卡片(.wb-cat-card),
// 不再是行列表;大数字改放入链数(越多入链说明这个概念被引用越多,
// 跟分类卡"大数字=笔记数"是同一种"越大越重要"的读法)。每天随机挑
// CONCEPT_PICK_N 个(不是固定按入链榜排序——固定榜单每次点开都是同一批
// 热门概念,起不到"发现冷门概念"的作用),总数不到这个数就全部show
// 不用随机。说明文字放在卡片网格下面(而不是像旧版"今日随机"提示那样
// 放在上面),按用户要求"下面加一句话"。
const CONCEPT_PICK_N = 9;
const conceptsPanel = (host) => {
  const conceptArr = concepts.array();
  const showAll = conceptArr.length <= CONCEPT_PICK_N;
  let pick = conceptArr;
  if (!showAll) {
    let seed = Math.floor(Date.now() / 86400000);
    const rand = () => {
      seed = (seed + 0x6D2B79F5) | 0;
      let t = Math.imul(seed ^ (seed >>> 15), 1 | seed);
      t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
      return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
    };
    const shuffled = [...conceptArr];
    for (let i = shuffled.length - 1; i > 0; i--) {
      const j = Math.floor(rand() * (i + 1));
      [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
    }
    pick = shuffled.slice(0, CONCEPT_PICK_N);
  }
  const grid = host.createEl("div", { cls: "wb-cat-grid" });
  for (const pg of pick) {
    const card = grid.createEl("div", { cls: "wb-cat-card", attr: { title: pg.file.name } });
    card.createEl("div", { cls: "wb-cat-num", text: String(pg.file.inlinks.length) });
    card.createEl("div", { cls: "wb-cat-name", text: pg.file.name });
    card.onclick = () => open(pg.file.path);
  }
  host.createEl("div", {
    cls: "wb-panel-caption",
    text: showAll
      ? `🌿 概念库还没到 ${CONCEPT_PICK_N} 个,全部在这了`
      : `🌿 今日概念 · 每天随机挑 ${CONCEPT_PICK_N} 个,过了今天换一批`,
  });
};

// 本周新增:用默认的 renderRows(超过 8 条会出"展开全部"),用户明确
// 要求保留这个能力,不要跟最近编辑一样硬顶——2026-07-07 一度改成硬顶
// 8 条,是误判了"➕新建笔记胶囊换行位置跳动"的根因是"本周新增展开后
// 页面变高";实际根因是 .wb-panel-row-title 缺 min-width:0 导致长标题
// 把日期挤出屏幕右边界(已在 workbench.css 修),跟本周新增展不展开
// 没关系,这个硬顶是误诊,撤销
const weekPanel = (host) => renderRows(host, [...weekNewPages].sort((a, b) => ctimeOf(b) - ctimeOf(a)), (pg) => fmt(ctimeOf(pg)));
// 最近编辑固定只给 8 条,不设"展开全部"——这颗胶囊本来就是"最近"这个
// 时间切片的意思,不是需要看全的列表,这条判断没有被推翻,继续保留
const recentPanel = (host) => renderRows(host, [...all].sort((a, b) => mtimeOf(b) - mtimeOf(a)).slice(0, 8), (pg) => fmt(mtimeOf(pg)));

const bar = dv.container.createEl("div", { cls: "wb-cards" });
const panel = dv.container.createEl("div", { cls: "wb-panel" });
panel.style.display = "none";

// 面板型胶囊(点了在下方展开/收起内容)复用这个 helper;双链是例外
// (见下面),不走这条路径,直接点击触发动作,不参与 activeCard 互斥
let activeCard = null;
const addTab = (icon, num, lbl, render) => {
  const card = bar.createEl("div", { cls: "wb-card" });
  card.createEl("span", { cls: "wb-num", text: num === "" ? icon : `${icon} ${num}` });
  card.createEl("span", { cls: "wb-lbl", text: lbl });
  card.onclick = () => {
    panel.innerHTML = "";
    if (activeCard === card) {
      // 再点当前亮着的胶囊 = 收起面板,回到干净的一排胶囊
      activeCard = null;
      card.classList.remove("is-active");
      panel.style.display = "none";
      return;
    }
    if (activeCard) activeCard.classList.remove("is-active");
    activeCard = card;
    card.classList.add("is-active");
    panel.style.display = "";
    render(panel);
  };
};

addTab("📝", notes.length, "笔记", notesPanel);
addTab("🌿", concepts.length, "概念", conceptsPanel);

// 双链:2026-07-07 用户要求这颗不展开页内面板,直接打开 Obsidian
// 原生图谱视图——图谱是独立的可视化,塞进页内小面板意义不大,枢纽
// 排行列表反而是画蛇添足,不如像旧版一样原地跳转更直接
const linksCard = bar.createEl("div", { cls: "wb-card" });
linksCard.createEl("span", { cls: "wb-num", text: `🕸️ ${edges}` });
linksCard.createEl("span", { cls: "wb-lbl", text: "双链" });
linksCard.onclick = openGraph;

addTab("🌱", weekNewPages.length, "本周新增", weekPanel);
addTab("🕘", "", "最近编辑", recentPanel);

// ➕ 新建笔记:跟其他胶囊同一个 .wb-card 外观,只在图标 span 上单独做
// "划过/点击转一圈"的动效(class 见 workbench.css 的 .wb-newnote-glyph)
const plus = bar.createEl("div", { cls: "wb-card wb-newnote" });
const plusGlyph = plus.createEl("span", { cls: "wb-num wb-newnote-glyph", text: "＋" });
plus.createEl("span", { cls: "wb-lbl", text: "新建笔记" });
plus.onclick = async () => {
  if (plus.classList.contains("is-busy")) return;
  plus.classList.add("is-busy");
  const anim = plusGlyph.animate(
    [{ transform: "rotate(0deg)" }, { transform: "rotate(360deg)" }],
    { duration: 450, easing: "ease" }
  );
  try { await anim.finished; } catch (_) { /* 动画被打断也照样建笔记 */ }
  plusGlyph.style.removeProperty("transform");
  plus.classList.remove("is-busy");
  newNote();
};
```

> [!draw] 🎴 今日抽卡 · 对抗遗忘
> [[notes/生活方式/命理定制-去寒排毒高能量每日方案|命理定制:去寒、排毒、高能量每日方案]] —— 07-17 录入。
>
> 先别点开,回顾一下这三点(不是对错测试,只是帮你想起笔记讲了什么):
>
> 1. 方案的底层逻辑是"白天借什么搞钱,晚上用什么安神"——具体是哪两个五行元素?
> 2. 晨间"借火唤醒"那三步分别是什么(一件跟喝有关,一件跟晒太阳有关,一件跟列清单有关)?
> 3. 主动要拉开距离的黑名单里,笔记具体点名了哪几种要立刻远离的人?
>
> 想不起来再点进去回顾原文。

> [!seed] 🌱 今日延伸 · 一个新知识点
> 关于 [[concepts/水缸|水缸]]:概念页第二条开放问题问——如果慢性渗透者始终不触发身体警报,单靠自己定期做跨时间基线对比是否够,还是需要引入一个不会被同一段关系悄悄污染的第三方视角。生态学里有个现成的类比叫**基线偏移综合征**(shifting baseline syndrome,渔业生态学家 Daniel Pauly 1995 年提出):每一代渔业研究者都把自己入行时看到的鱼群规模当作"正常基准",却不知道那本身已经是几十年持续衰退后的低点——每代人的"正常"标准也在同步下滑,单靠自己回忆对比测不出这种缓慢的整体衰退,因为参照系本身已经被污染了。放到水缸的问题上:如果一段关系在缓慢变差,你对"正常"的感受基准很可能也在被这段关系本身悄悄下调,拿"现在"跟"记忆里的过去"对比时,那份记忆本身可能早就不干净了。[事实,shifting baseline syndrome 是渔业生态学的真实概念,类比部分未联网核查]
>
> 家暴/胁迫控制研究(coercive control,Evan Stark)里一个反复被记录的施害模式,是主动切断受害者与外部社交圈的联系——孤立本身造成的痛苦是一层,更关键的是它顺带消灭了那个"未被这段关系污染的基线"来源:身边没有一个人可以对照时,受害者只能拿自己已经被重新校准过的记忆当基准。这从反面印证了"需要第三方视角"不是锦上添花,而是慢性渗透真正生效时最先被拿掉的那道防线。[推论,基于胁迫控制文献与该开放问题的类比,未联网核查]
>
> 这两条已经同步写进了 [[concepts/水缸|水缸]] 概念页的延伸区。

> [!ask] ❓ 问题1
> [[concepts/客服模式|客服模式]]的开放问题问:敲门砖不可避免(关系总要有个具体的由头),从"功能开门"到真正撕掉标签,这段转换窗口能有多长?概念页延伸区提到**社会渗透理论**——亲密度不是靠认识多久堆出来的,而是靠双方有没有持续把自我暴露从"你是做什么的"这类外围信息,往"我在怕什么"这类核心层推进。具体到你自己:有没有一段关系,一开始纯粹因为对方某个具体功能(帮你解决问题、能提供的资源、稳定的情绪价值)才建立起来,后来真的越过了只讲交换这一层?那个转折点,你现在回想起来发生在哪个具体时刻?反过来,有没有一段关系你自己清楚已经卡在客服模式很久了,双方都心知肚明只剩功能价值,却还维持着?
>
> > [!write] ✍️ 你的回答
> >我觉得是有的，就是在公示公办的时候也能察觉到对方的性格，如果是我喜欢的，我会主动尝试更深入，当然也有只有功能价值的人，这是人必要的社会资源。
> >
>
> > [!note]- 💬 我的看法
> > 我倾向于觉得转折点很少是当下就能意识到的——真正的核心层自我暴露往往发生在一方毫无防备、状态最狼狈的时刻(生病、崩溃、犯错被抓包),不是刻意选的"我信任你了所以跟你说点心里话"。反而是那些明明想跨过这层、却挑状态最体面的时刻精心安排一次"深谈"的关系,更容易还停留在客服模式——因为连"什么时候暴露"都还在被管理,管理本身就是功能性思维没退场的证据。[观点]
> >
> > 这也是为什么"明知卡住却还维持"这种情况更值得警惕:它往往不是没能力往深处推进,而是双方都默契地把关系锁在一个"够用"的功能层上,谁也不愿意先冒险暴露脆弱——维持现状比可能撕破脸更安全。[推论]

> [!ask] ❓ 问题2
> [[concepts/含情注视|含情注视]]的开放问题问:含情注视和"理性复盘暴露了什么漏洞"(完整手册里的延后反省)方向正相反——一个要求先给自己"看啊你已经在尽力了"的宽容目光,一个要求先冷静剖开自己的漏洞在哪。这两个动作怎么共存,先后顺序是什么?具体到你自己:上一次真的搞砸一件事(不管是工作失误还是关系里的事)之后,你实际的第一反应是先安抚自己,还是先开始复盘找漏洞?如果两个都做了,中间隔了多久,是自然发生的,还是你刻意逼自己先停一停?
>
> > [!write] ✍️ 你的回答
> >
> >
>
> > [!note]- 💬 我的看法
> > 我倾向于认为顺序不是选择题,是生理限制——含情注视概念页延伸区提到 Paul Gilbert 的三系统情绪调节模型:处在威胁/驱动系统主导的高唤起状态时,靠前额叶主导的理性复盘本身做不准,搞砸后的第一反应通常带着应激,这时候强行复盘容易得出"我就是不行"这类被情绪污染的结论,而不是真正客观的漏洞清单。所以含情注视不是延后反省的对立面,更像是它的前置条件——先把系统从威胁状态里捞出来,复盘才有机会是理性的,不是发泄。[观点,基于含情注视概念页已有的三系统情绪调节模型类比]
> >
> > 如果两者中间完全没有间隔,直接从搞砸跳进复盘,这本身可能就是个信号——说明安抚这一步被跳过了,而不是真的不需要。[推论]
